// Stub of the NLua LuaThread type (the real one lives in the NLua package
// referenced by BizHawk.Client.Common). Only the empty shell is needed — the
// toolset never touches the thread itself.
namespace NLua
{
	public class LuaThread
	{
	}
}
